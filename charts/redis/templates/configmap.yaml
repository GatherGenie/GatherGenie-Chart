apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-config
  namespace: {{ .Release.Namespace }}
data:
  REDIS_HOST: "{{ .Values.config.REDIS_HOST }}"
  REDIS_PORT: "{{ .Values.config.REDIS_PORT }}"