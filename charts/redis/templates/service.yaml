apiVersion: v1
kind: Service
metadata:
  name: notification-redis
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: notification-redis
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 6379
  clusterIP: None